# Aditi Sharma Portfolio — Local Notes

Changes made on behalf of request (Nov 9, 2025):

- Added `resume.html` (lightweight resume page) and `resume.txt` placeholder for download. Replace `resume.txt` with `resume.pdf` to supply a real PDF.
- Added a "Resume" link to the main navigation in `index.html`.
- Added a new project entry for **Loopies** with description "building a community chat platform" and a GitHub link.
- Added "Details" buttons to projects and implemented a modal to show project details.
- Added small UI interactions: smooth scroll (already present), button press feedback, hover transforms, and modal open/close.
- Ensured existing buttons and project GitHub links open in new tabs.

How to test locally:

1. Open `index.html` in a browser (double-click or serve with a simple static server).
2. Click "Resume" in the nav to open `resume.html` in a new tab.
3. In Projects section: click "View on GitHub" to open repository, or click "Details" to open the modal.

Notes:
- I kept the existing color scheme and added only structural and interactive enhancements.
- Replace `resume.txt` with a real `resume.pdf` in the project root if you want a downloadable PDF.
